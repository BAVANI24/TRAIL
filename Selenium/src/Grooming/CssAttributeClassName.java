package Grooming;

import java.awt.Dimension;

import org.openqa.selenium.By;
import org.openqa.selenium.Point;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import io.opentelemetry.sdk.metrics.data.PointData;

public class CssAttributeClassName {
	//public static void main(String[] args) {
		/*
		 * WebDriver driver=new ChromeDriver(); driver.manage().window().maximize();
		 * driver.get("https://demowebshop.tricentis.com/");
		 * driver.findElement(By.className("search-box-text")).sendKeys("book");
		 * //driver.findElement(By.cssSelector(".search-box-text.ui-autocomplete-input")
		 * ).sendKeys("computer"); driver.findElement(By.
		 * xpath("//input[@class='search-box-text ui-autocomplete-input']")).sendKeys(
		 * "hiii");
		 * 
		 * Point axis=driver.findElement(By.
		 * xpath("//input[@class='search-box-text ui-autocomplete-input']")).getLocation
		 * (); int x=axis.getX(); int y=axis.getY();
		 * 
		 * System.out.println("X axis:"+x); System.out.println("y axis:"+y);
		 * 
		 * Dimension size=driver.findElement(By.
		 * xpath("//input[@class='search-box-text ui-autocomplete-input']")).getSize();
		 * double height=size.getHeight(); double width=size.getWidth();
		 * 
		 * System.out.println("height:"+height); System.out.println("width:"+width);
		 * 
		 * Recdriver.findElement(By.
		 * xpath("//input[@class='search-box-text ui-autocomplete-input']")).getRect();
		 * 
		 * 
		 * driver.close();
		 */
	//}
}
